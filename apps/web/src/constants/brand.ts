/**
 * Brand identity + support address for the shared public pages
 * (Privacy/Terms/ReportProblem wrappers). One declaration for the app --
 * router.tsx and page wrappers both consume it.
 */
export const BRAND = 'Sync/Sit';
export const SUPPORT_EMAIL = 'support@sync-sit.com';

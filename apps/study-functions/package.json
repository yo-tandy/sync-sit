{
  "name": "study-functions",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'no eslint config for study-functions yet' && exit 0",
    "predeploy": "node ../../scripts/bundle-shared-for-deploy.js"
  },
  "dependencies": {
    "@ejm/sit-core": "workspace:*",
    "@ejm/shared-core": "workspace:*",
    "@ejm/study-core": "workspace:*",
    "@ejm/shared-functions": "workspace:*",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "resend": "^4.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": "20"
  }
}
